# AI Prompt Contract for OCR

- Prompt must describe the image and request text extraction
- Must support multi-language images
- Should return text and confidence
- Example prompt:
  "Extract all readable text from the uploaded image. Return the text and a confidence score."
